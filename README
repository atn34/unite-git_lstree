Quick unite plugin for using git ls-tree for finding files.

A lot like the file_rec source, except it only presents options
under version control with git, which presumably are the files
you're most interested in editing.

Usage:

    :Unite git_lstree
